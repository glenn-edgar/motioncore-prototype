# Point-to-point bring-up: a single slave at RS-485 addr 1
# (slave's addr = low byte of its commissioned instance_id; bench slave = inst 1).
poll  500 3 2
slave 1 0x5E589100 0x02    # ENABLED, UDP (point-to-point TTL, no TCP yet)
